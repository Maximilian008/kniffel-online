# Feature flags for the new start flow
VITE_NEW_START_FLOW=0
# Backend base URL (leer = gleicher Origin)
VITE_API_BASE=
# Room adapter: mock | http  (http erfordert laufende /api/rooms* Endpoints)
VITE_ROOM_ADAPTER=mock
# disable sockets while new flow ships without realtime
VITE_DISABLE_SOCKETS=1
